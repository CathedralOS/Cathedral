// source/drivers/facts/build.omg
//
// Hardware facts are a named pure-data package with no service grants.

machine build(builder: &mut Build) {
    builder.package("cathedral-hardware-facts");
    // facts reach nothing; no targets.
}
