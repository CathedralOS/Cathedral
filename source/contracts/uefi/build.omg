// source/contracts/uefi/build.omg
//
// The UEFI ABI contracts package. Reaches nothing and uses default settings, so
// `build` augments nothing. (A package with no deps and default settings could
// omit build.omg entirely — ZII gives the defaults — but it is kept explicit
// here.) PROVISIONAL SYNTAX (Omega build_and_package_model.md).

machine build(b: &mut Build) {
    // contracts reach nothing; no targets to augment.
}
