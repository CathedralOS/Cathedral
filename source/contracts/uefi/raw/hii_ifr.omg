// SPDX-License-Identifier: MIT OR Apache-2.0
// Modified rust-osdev/uefi-rs translation at c0facddf9ba42b74906a37fca2869e6cdbc8da6a.
// See hii.PORT.md. Inert carriers; pointer/slot bits confer no authority.
module hii_ifr;
use scalars;
use console;

// Upstream: uefi_raw::protocol::hii::ifr::IfrType
pub data IfrType [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrTypeValue
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrTypeValueWireStorage [copy] {
    bytes: [u8; 22];
    // Blocked overlay member u8: u8; upstream offset 0.
    // Blocked overlay member u16: u16; upstream offset 0.
    // Blocked overlay member u32: u32; upstream offset 0.
    // Blocked overlay member u64: u64; upstream offset 0.
    // Blocked overlay member b: Boolean; upstream offset 0.
    // Blocked overlay member time: HiiTime; upstream offset 0.
    // Blocked overlay member date: HiiDate; upstream offset 0.
    // Blocked overlay member string: StringId; upstream offset 0.
    // Blocked overlay member hii_ref: HiiRef; upstream offset 0.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOpcode
pub data IfrOpcode [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOpHeader
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOpHeader [copy] {
    opcode: IfrOpcode;
    length_and_scope: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrStatementHeader
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrStatementHeader [copy] {
    prompt: u16;
    help: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::VarstoreInfo
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data VarstoreInfoWireStorage [copy] {
    bytes: [u8; 2];
    // Blocked overlay member var_name: StringId; upstream offset 0.
    // Blocked overlay member var_offset: u16; upstream offset 0.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionFlags
pub data IfrQuestionFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionHeader
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrQuestionHeaderWireStorage [copy] {
    header: IfrStatementHeader;
    question_id: u16;
    varstore_id: u16;
    varstore_info_storage: VarstoreInfoWireStorage;
    flags: IfrQuestionFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrAction
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrActionWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    question_config: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrAction1
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrAction1WireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrAnimation
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrAnimation [copy] {
    header: IfrOpHeader;
    id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrAdd
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrAdd [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrAnd
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrAnd [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrBitwiseAnd
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrBitwiseAnd [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrBitwiseNot
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrBitwiseNot [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrBitwiseOr
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrBitwiseOr [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrCatenate
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrCatenate [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrCheckboxFlags
pub data IfrCheckboxFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrCheckbox
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrCheckboxWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    flags: IfrCheckboxFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrConditional
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrConditional [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDateFlags
pub data IfrDateFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDateStorage
pub data IfrDateStorage [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDate
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDateWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    flags: IfrDateFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDefault
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDefaultWireStorage [copy] {
    header: IfrOpHeader;
    default_id: u16;
    type_code: IfrType;
    value_storage: IfrTypeValueWireStorage;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDefault2
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDefault2 [copy] {
    header: IfrOpHeader;
    default_id: u16;
    type_code: IfrType;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDefaultStore
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDefaultStore [copy] {
    header: IfrOpHeader;
    default_name: u16;
    default_id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDisableIf
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDisableIf [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDivide
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDivide [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrDup
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrDup [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrEnd
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrEnd [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrEqual
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrEqual [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrEqIdId
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrEqIdId [copy] {
    header: IfrOpHeader;
    question_id1: u16;
    question_id2: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrEqIdValList
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrEqIdValList [copy] {
    header: IfrOpHeader;
    question_id: u16;
    list_length: u16;
    // PORT-BLOCKED[omega:runtime-layout-strides]: typed trailing value_list ([u16; 0]) requires bounded runtime shape.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrEqIdVal
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrEqIdVal [copy] {
    header: IfrOpHeader;
    question_id: u16;
    value: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrFalse
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrFalse [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrFindFormat
pub data IfrFindFormat [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrFind
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrFind [copy] {
    header: IfrOpHeader;
    format: IfrFindFormat;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrForm
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrForm [copy] {
    header: IfrOpHeader;
    form_id: u16;
    form_title: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrFormMapMethod
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrFormMapMethod [copy] {
    method_title: u16;
    method_identifier: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrFormMap
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrFormMap [copy] {
    header: IfrOpHeader;
    form_id: u16;
    // PORT-BLOCKED[omega:runtime-layout-strides]: typed trailing methods ([IfrFormMapMethod; 0]) requires bounded runtime shape.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrFormSet
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrFormSet [copy] {
    header: IfrOpHeader;
    guid: scalars::Guid;
    formset_title: u16;
    help: u16;
    flags: u8;
    // PORT-BLOCKED[omega:runtime-layout-strides]: typed trailing class_guid ([Guid; 0]) requires bounded runtime shape.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrGet
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrGetWireStorage [copy] {
    header: IfrOpHeader;
    varstore_id: u16;
    varstore_info_storage: VarstoreInfoWireStorage;
    varstore_type: IfrType;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrGrayOutIf
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrGrayOutIf [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrGreaterEqual
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrGreaterEqual [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrGreaterThan
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrGreaterThan [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrGuid
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrGuid [copy] {
    header: IfrOpHeader;
    guid: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrImage
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrImage [copy] {
    header: IfrOpHeader;
    id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrInconsistentIf
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrInconsistentIf [copy] {
    header: IfrOpHeader;
    error: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrLength
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrLength [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrLessEqual
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrLessEqual [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrLessThan
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrLessThan [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrLocked
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrLocked [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrMap
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrMap [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrMatch
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrMatch [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrMatch2
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrMatch2 [copy] {
    header: IfrOpHeader;
    syntax_type: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrMid
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrMid [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrModalTag
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrModalTag [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrModulo
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrModulo [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrMultiply
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrMultiply [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNot
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNot [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNotEqual
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNotEqual [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNoSubmitIf
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNoSubmitIf [copy] {
    header: IfrOpHeader;
    error: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericU8
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNumericU8 [copy] {
    min_value: u8;
    max_value: u8;
    step: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericU16
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNumericU16 [copy] {
    min_value: u16;
    max_value: u16;
    step: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericU32
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNumericU32 [copy] {
    min_value: u32;
    max_value: u32;
    step: u32;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericU64
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNumericU64 [copy] {
    min_value: u64;
    max_value: u64;
    step: u64;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericData
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNumericDataWireStorage [copy] {
    bytes: [u8; 24];
    // Blocked overlay member u8: IfrNumericU8; upstream offset 0.
    // Blocked overlay member u16: IfrNumericU16; upstream offset 0.
    // Blocked overlay member u32: IfrNumericU32; upstream offset 0.
    // Blocked overlay member u64: IfrNumericU64; upstream offset 0.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericSize
pub data IfrNumericSize [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericDisplay
pub data IfrNumericDisplay [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumericFlags
pub data IfrNumericFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrNumeric
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrNumericWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    flags: IfrNumericFlags;
    data_storage: IfrNumericDataWireStorage;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOne
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOne [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOnes
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOnes [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOneOf
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOneOfWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    flags: IfrNumericFlags;
    data_storage: IfrNumericDataWireStorage;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOptionFlags
pub data IfrOptionFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOneOfOption
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOneOfOptionWireStorage [copy] {
    header: IfrOpHeader;
    option: u16;
    flags: IfrOptionFlags;
    type_code: IfrType;
    value_storage: IfrTypeValueWireStorage;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOr
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOr [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOrderedListFlags
pub data IfrOrderedListFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrOrderedList
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrOrderedListWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    max_containers: u8;
    flags: IfrOrderedListFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrPassword
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrPasswordWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    min_size: u16;
    max_size: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionRef1
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrQuestionRef1 [copy] {
    header: IfrOpHeader;
    question_id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionRef2
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrQuestionRef2 [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionRef3
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrQuestionRef3 [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionRef32
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrQuestionRef32 [copy] {
    header: IfrOpHeader;
    device_path: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrQuestionRef33
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrQuestionRef33 [copy] {
    header: IfrOpHeader;
    device_path: u16;
    guid: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRead
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRead [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRef
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRefWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    form_id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRef2
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRef2WireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    form_id: u16;
    question_id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRef3
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRef3WireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    form_id: u16;
    question_id: u16;
    formset_id: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRef4
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRef4WireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    form_id: u16;
    question_id: u16;
    formset_id: scalars::Guid;
    device_path: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRef5
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRef5WireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRefresh
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRefresh [copy] {
    header: IfrOpHeader;
    refresh_interval: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRefreshId
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRefreshId [copy] {
    header: IfrOpHeader;
    refresh_event_group_id: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrResetButton
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrResetButton [copy] {
    header: IfrOpHeader;
    statement: IfrStatementHeader;
    default_id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRule
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRule [copy] {
    header: IfrOpHeader;
    rule_id: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrRuleRef
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrRuleRef [copy] {
    header: IfrOpHeader;
    rule_id: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSecurity
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrSecurity [copy] {
    header: IfrOpHeader;
    permissions: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSet
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrSetWireStorage [copy] {
    header: IfrOpHeader;
    varstore_id: u16;
    varstore_info_storage: VarstoreInfoWireStorage;
    varstore_type: IfrType;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrShiftLeft
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrShiftLeft [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrShiftRight
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrShiftRight [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSpan
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrSpan [copy] {
    header: IfrOpHeader;
    flags: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrStringFlags
pub data IfrStringFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrString
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrStringWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    min_size: u8;
    max_size: u8;
    flags: IfrStringFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrStringRef1
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrStringRef1 [copy] {
    header: IfrOpHeader;
    string_id: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrStringRef2
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrStringRef2 [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSubtitleFlags
pub data IfrSubtitleFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSubtitle
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrSubtitle [copy] {
    header: IfrOpHeader;
    statement: IfrStatementHeader;
    flags: IfrSubtitleFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSubtract
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrSubtract [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrSuppressIf
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrSuppressIf [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrText
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrText [copy] {
    header: IfrOpHeader;
    statement: IfrStatementHeader;
    text_two: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrThis
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrThis [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrTimeFlags
pub data IfrTimeFlags [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrTimeStorage
pub data IfrTimeStorage [copy] {
    raw: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrTime
// PORT-BLOCKED[omega:programmable-overlays]: storage bytes are not a typed union or a validated IFR value.
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrTimeWireStorage [copy] {
    header: IfrOpHeader;
    question_storage: IfrQuestionHeaderWireStorage;
    flags: IfrTimeFlags;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrToken
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrToken [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrToBoolean
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrToBoolean [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrToLower
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrToLower [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrToString
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrToString [copy] {
    header: IfrOpHeader;
    format: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrToUint
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrToUint [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrToUpper
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrToUpper [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrTrue
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrTrue [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrUint8
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrUint8 [copy] {
    header: IfrOpHeader;
    value: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrUint16
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrUint16 [copy] {
    header: IfrOpHeader;
    value: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrUint32
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrUint32 [copy] {
    header: IfrOpHeader;
    value: u32;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrUint64
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrUint64 [copy] {
    header: IfrOpHeader;
    value: u64;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrUndefined
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrUndefined [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrValue
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrValue [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrVarstore
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrVarstore [copy] {
    header: IfrOpHeader;
    guid: scalars::Guid;
    varstore_id: u16;
    size: u16;
    // PORT-BLOCKED[omega:runtime-layout-strides]: typed trailing name ([u8; 0]) requires bounded runtime shape.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrVarstoreNameValue
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrVarstoreNameValue [copy] {
    header: IfrOpHeader;
    varstore_id: u16;
    guid: scalars::Guid;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrVarstoreEfi
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrVarstoreEfi [copy] {
    header: IfrOpHeader;
    varstore_id: u16;
    guid: scalars::Guid;
    attributes: u32;
    size: u16;
    // PORT-BLOCKED[omega:runtime-layout-strides]: typed trailing name ([u8; 0]) requires bounded runtime shape.
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrVarstoreDevice
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrVarstoreDevice [copy] {
    header: IfrOpHeader;
    device_path: u16;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrVersion
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrVersion [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrWarningIf
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrWarningIf [copy] {
    header: IfrOpHeader;
    warning: u16;
    timeout: u8;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrWrite
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrWrite [copy] {
    header: IfrOpHeader;
}

// Upstream: uefi_raw::protocol::hii::ifr::IfrZero
// Required packed layout: alignment 1, no inter-field padding.
pub data IfrZero [copy] {
    header: IfrOpHeader;
}

pub const IFR_TYPE_NUM_SIZE_8: IfrType = IfrType { raw: 0 };
pub const IFR_TYPE_NUM_SIZE_16: IfrType = IfrType { raw: 1 };
pub const IFR_TYPE_NUM_SIZE_32: IfrType = IfrType { raw: 2 };
pub const IFR_TYPE_NUM_SIZE_64: IfrType = IfrType { raw: 3 };
pub const IFR_TYPE_BOOLEAN: IfrType = IfrType { raw: 4 };
pub const IFR_TYPE_TIME: IfrType = IfrType { raw: 5 };
pub const IFR_TYPE_DATE: IfrType = IfrType { raw: 6 };
pub const IFR_TYPE_STRING: IfrType = IfrType { raw: 7 };
pub const IFR_TYPE_OTHER: IfrType = IfrType { raw: 8 };
pub const IFR_TYPE_UNDEFINED: IfrType = IfrType { raw: 9 };
pub const IFR_TYPE_ACTION: IfrType = IfrType { raw: 10 };
pub const IFR_TYPE_BUFFER: IfrType = IfrType { raw: 11 };
pub const IFR_TYPE_REF: IfrType = IfrType { raw: 12 };
pub const IFR_OPCODE_FORM: IfrOpcode = IfrOpcode { raw: 1 };
pub const IFR_OPCODE_SUBTITLE: IfrOpcode = IfrOpcode { raw: 2 };
pub const IFR_OPCODE_TEXT: IfrOpcode = IfrOpcode { raw: 3 };
pub const IFR_OPCODE_IMAGE: IfrOpcode = IfrOpcode { raw: 4 };
pub const IFR_OPCODE_ONE_OF: IfrOpcode = IfrOpcode { raw: 5 };
pub const IFR_OPCODE_CHECKBOX: IfrOpcode = IfrOpcode { raw: 6 };
pub const IFR_OPCODE_NUMERIC: IfrOpcode = IfrOpcode { raw: 7 };
pub const IFR_OPCODE_PASSWORD: IfrOpcode = IfrOpcode { raw: 8 };
pub const IFR_OPCODE_ONE_OF_OPTION: IfrOpcode = IfrOpcode { raw: 9 };
pub const IFR_OPCODE_SUPPRESS_IF: IfrOpcode = IfrOpcode { raw: 10 };
pub const IFR_OPCODE_LOCKED: IfrOpcode = IfrOpcode { raw: 11 };
pub const IFR_OPCODE_ACTION: IfrOpcode = IfrOpcode { raw: 12 };
pub const IFR_OPCODE_RESET_BUTTON: IfrOpcode = IfrOpcode { raw: 13 };
pub const IFR_OPCODE_FORM_SET: IfrOpcode = IfrOpcode { raw: 14 };
pub const IFR_OPCODE_REF: IfrOpcode = IfrOpcode { raw: 15 };
pub const IFR_OPCODE_NO_SUBMIT_IF: IfrOpcode = IfrOpcode { raw: 16 };
pub const IFR_OPCODE_INCONSISTENT_IF: IfrOpcode = IfrOpcode { raw: 17 };
pub const IFR_OPCODE_EQ_ID_VAL: IfrOpcode = IfrOpcode { raw: 18 };
pub const IFR_OPCODE_EQ_ID_ID: IfrOpcode = IfrOpcode { raw: 19 };
pub const IFR_OPCODE_EQ_ID_VAL_LIST: IfrOpcode = IfrOpcode { raw: 20 };
pub const IFR_OPCODE_AND: IfrOpcode = IfrOpcode { raw: 21 };
pub const IFR_OPCODE_OR: IfrOpcode = IfrOpcode { raw: 22 };
pub const IFR_OPCODE_NOT: IfrOpcode = IfrOpcode { raw: 23 };
pub const IFR_OPCODE_RULE: IfrOpcode = IfrOpcode { raw: 24 };
pub const IFR_OPCODE_GRAY_OUT_IF: IfrOpcode = IfrOpcode { raw: 25 };
pub const IFR_OPCODE_DATE: IfrOpcode = IfrOpcode { raw: 26 };
pub const IFR_OPCODE_TIME: IfrOpcode = IfrOpcode { raw: 27 };
pub const IFR_OPCODE_STRING: IfrOpcode = IfrOpcode { raw: 28 };
pub const IFR_OPCODE_REFRESH: IfrOpcode = IfrOpcode { raw: 29 };
pub const IFR_OPCODE_DISABLE_IF: IfrOpcode = IfrOpcode { raw: 30 };
pub const IFR_OPCODE_ANIMATION: IfrOpcode = IfrOpcode { raw: 31 };
pub const IFR_OPCODE_TO_LOWER: IfrOpcode = IfrOpcode { raw: 32 };
pub const IFR_OPCODE_TO_UPPER: IfrOpcode = IfrOpcode { raw: 33 };
pub const IFR_OPCODE_MAP: IfrOpcode = IfrOpcode { raw: 34 };
pub const IFR_OPCODE_ORDERED_LIST: IfrOpcode = IfrOpcode { raw: 35 };
pub const IFR_OPCODE_VARSTORE: IfrOpcode = IfrOpcode { raw: 36 };
pub const IFR_OPCODE_VARSTORE_NAME_VALUE: IfrOpcode = IfrOpcode { raw: 37 };
pub const IFR_OPCODE_VARSTORE_EFI: IfrOpcode = IfrOpcode { raw: 38 };
pub const IFR_OPCODE_VARSTORE_DEVICE: IfrOpcode = IfrOpcode { raw: 39 };
pub const IFR_OPCODE_VERSION: IfrOpcode = IfrOpcode { raw: 40 };
pub const IFR_OPCODE_END: IfrOpcode = IfrOpcode { raw: 41 };
pub const IFR_OPCODE_MATCH: IfrOpcode = IfrOpcode { raw: 42 };
pub const IFR_OPCODE_GET: IfrOpcode = IfrOpcode { raw: 43 };
pub const IFR_OPCODE_SET: IfrOpcode = IfrOpcode { raw: 44 };
pub const IFR_OPCODE_READ: IfrOpcode = IfrOpcode { raw: 45 };
pub const IFR_OPCODE_WRITE: IfrOpcode = IfrOpcode { raw: 46 };
pub const IFR_OPCODE_EQUAL: IfrOpcode = IfrOpcode { raw: 47 };
pub const IFR_OPCODE_NOT_EQUAL: IfrOpcode = IfrOpcode { raw: 48 };
pub const IFR_OPCODE_GREATER_THAN: IfrOpcode = IfrOpcode { raw: 49 };
pub const IFR_OPCODE_GREATER_EQUAL: IfrOpcode = IfrOpcode { raw: 50 };
pub const IFR_OPCODE_LESS_THAN: IfrOpcode = IfrOpcode { raw: 51 };
pub const IFR_OPCODE_LESS_EQUAL: IfrOpcode = IfrOpcode { raw: 52 };
pub const IFR_OPCODE_BITWISE_AND: IfrOpcode = IfrOpcode { raw: 53 };
pub const IFR_OPCODE_BITWISE_OR: IfrOpcode = IfrOpcode { raw: 54 };
pub const IFR_OPCODE_BITWISE_NOT: IfrOpcode = IfrOpcode { raw: 55 };
pub const IFR_OPCODE_SHIFT_LEFT: IfrOpcode = IfrOpcode { raw: 56 };
pub const IFR_OPCODE_SHIFT_RIGHT: IfrOpcode = IfrOpcode { raw: 57 };
pub const IFR_OPCODE_ADD: IfrOpcode = IfrOpcode { raw: 58 };
pub const IFR_OPCODE_SUBTRACT: IfrOpcode = IfrOpcode { raw: 59 };
pub const IFR_OPCODE_MULTIPLY: IfrOpcode = IfrOpcode { raw: 60 };
pub const IFR_OPCODE_DIVIDE: IfrOpcode = IfrOpcode { raw: 61 };
pub const IFR_OPCODE_MODULO: IfrOpcode = IfrOpcode { raw: 62 };
pub const IFR_OPCODE_RULE_REF: IfrOpcode = IfrOpcode { raw: 63 };
pub const IFR_OPCODE_QUESTION_REF1: IfrOpcode = IfrOpcode { raw: 64 };
pub const IFR_OPCODE_QUESTION_REF2: IfrOpcode = IfrOpcode { raw: 65 };
pub const IFR_OPCODE_UINT8: IfrOpcode = IfrOpcode { raw: 66 };
pub const IFR_OPCODE_UINT16: IfrOpcode = IfrOpcode { raw: 67 };
pub const IFR_OPCODE_UINT32: IfrOpcode = IfrOpcode { raw: 68 };
pub const IFR_OPCODE_UINT64: IfrOpcode = IfrOpcode { raw: 69 };
pub const IFR_OPCODE_TRUE: IfrOpcode = IfrOpcode { raw: 70 };
pub const IFR_OPCODE_FALSE: IfrOpcode = IfrOpcode { raw: 71 };
pub const IFR_OPCODE_TO_UINT: IfrOpcode = IfrOpcode { raw: 72 };
pub const IFR_OPCODE_TO_STRING: IfrOpcode = IfrOpcode { raw: 73 };
pub const IFR_OPCODE_TO_BOOLEAN: IfrOpcode = IfrOpcode { raw: 74 };
pub const IFR_OPCODE_MID: IfrOpcode = IfrOpcode { raw: 75 };
pub const IFR_OPCODE_FIND: IfrOpcode = IfrOpcode { raw: 76 };
pub const IFR_OPCODE_TOKEN: IfrOpcode = IfrOpcode { raw: 77 };
pub const IFR_OPCODE_STRING_REF1: IfrOpcode = IfrOpcode { raw: 78 };
pub const IFR_OPCODE_STRING_REF2: IfrOpcode = IfrOpcode { raw: 79 };
pub const IFR_OPCODE_CONDITIONAL: IfrOpcode = IfrOpcode { raw: 80 };
pub const IFR_OPCODE_QUESTION_REF3: IfrOpcode = IfrOpcode { raw: 81 };
pub const IFR_OPCODE_ZERO: IfrOpcode = IfrOpcode { raw: 82 };
pub const IFR_OPCODE_ONE: IfrOpcode = IfrOpcode { raw: 83 };
pub const IFR_OPCODE_ONES: IfrOpcode = IfrOpcode { raw: 84 };
pub const IFR_OPCODE_UNDEFINED: IfrOpcode = IfrOpcode { raw: 85 };
pub const IFR_OPCODE_LENGTH: IfrOpcode = IfrOpcode { raw: 86 };
pub const IFR_OPCODE_DUP: IfrOpcode = IfrOpcode { raw: 87 };
pub const IFR_OPCODE_THIS: IfrOpcode = IfrOpcode { raw: 88 };
pub const IFR_OPCODE_SPAN: IfrOpcode = IfrOpcode { raw: 89 };
pub const IFR_OPCODE_VALUE: IfrOpcode = IfrOpcode { raw: 90 };
pub const IFR_OPCODE_DEFAULT: IfrOpcode = IfrOpcode { raw: 91 };
pub const IFR_OPCODE_DEFAULTSTORE: IfrOpcode = IfrOpcode { raw: 92 };
pub const IFR_OPCODE_FORM_MAP: IfrOpcode = IfrOpcode { raw: 93 };
pub const IFR_OPCODE_CATENATE: IfrOpcode = IfrOpcode { raw: 94 };
pub const IFR_OPCODE_GUID: IfrOpcode = IfrOpcode { raw: 95 };
pub const IFR_OPCODE_SECURITY: IfrOpcode = IfrOpcode { raw: 96 };
pub const IFR_OPCODE_MODAL_TAG: IfrOpcode = IfrOpcode { raw: 97 };
pub const IFR_OPCODE_REFRESH_ID: IfrOpcode = IfrOpcode { raw: 98 };
pub const IFR_OPCODE_WARNING_IF: IfrOpcode = IfrOpcode { raw: 99 };
pub const IFR_OPCODE_MATCH2: IfrOpcode = IfrOpcode { raw: 100 };
pub const IFR_QUESTION_FLAGS_READ_ONLY: IfrQuestionFlags = IfrQuestionFlags { raw: 1 };
pub const IFR_QUESTION_FLAGS_CALLBACK: IfrQuestionFlags = IfrQuestionFlags { raw: 4 };
pub const IFR_QUESTION_FLAGS_RESET_REQUIRED: IfrQuestionFlags = IfrQuestionFlags { raw: 16 };
pub const IFR_QUESTION_FLAGS_REST_STYLE: IfrQuestionFlags = IfrQuestionFlags { raw: 32 };
pub const IFR_QUESTION_FLAGS_RECONNECT_REQUIRED: IfrQuestionFlags = IfrQuestionFlags { raw: 64 };
pub const IFR_QUESTION_FLAGS_OPTIONS_ONLY: IfrQuestionFlags = IfrQuestionFlags { raw: 128 };
pub const IFR_CHECKBOX_FLAGS_DEFAULT: IfrCheckboxFlags = IfrCheckboxFlags { raw: 1 };
pub const IFR_CHECKBOX_FLAGS_DEFAULT_MFG: IfrCheckboxFlags = IfrCheckboxFlags { raw: 2 };
pub const IFR_DATE_FLAGS_YEAR_SUPPRESS: IfrDateFlags = IfrDateFlags { raw: 1 };
pub const IFR_DATE_FLAGS_MONTH_SUPPRESS: IfrDateFlags = IfrDateFlags { raw: 2 };
pub const IFR_DATE_FLAGS_DAY_SUPPRESS: IfrDateFlags = IfrDateFlags { raw: 4 };
pub const IFR_DATE_FLAGS_STORAGE: IfrDateFlags = IfrDateFlags { raw: 48 };
pub const IFR_DATE_STORAGE_NORMAL: IfrDateStorage = IfrDateStorage { raw: 0 };
pub const IFR_DATE_STORAGE_TIME: IfrDateStorage = IfrDateStorage { raw: 16 };
pub const IFR_DATE_STORAGE_WAKEUP: IfrDateStorage = IfrDateStorage { raw: 32 };
pub const IFR_FIND_FORMAT_SENSITIVE: IfrFindFormat = IfrFindFormat { raw: 0 };
pub const IFR_FIND_FORMAT_INSENSITIVE: IfrFindFormat = IfrFindFormat { raw: 1 };
pub const IFR_NUMERIC_SIZE_SIZE_1: IfrNumericSize = IfrNumericSize { raw: 0 };
pub const IFR_NUMERIC_SIZE_SIZE_2: IfrNumericSize = IfrNumericSize { raw: 1 };
pub const IFR_NUMERIC_SIZE_SIZE_4: IfrNumericSize = IfrNumericSize { raw: 2 };
pub const IFR_NUMERIC_SIZE_SIZE_8: IfrNumericSize = IfrNumericSize { raw: 3 };
pub const IFR_NUMERIC_DISPLAY_INT_DEC: IfrNumericDisplay = IfrNumericDisplay { raw: 0 };
pub const IFR_NUMERIC_DISPLAY_UINT_DEC: IfrNumericDisplay = IfrNumericDisplay { raw: 16 };
pub const IFR_NUMERIC_DISPLAY_UINT_HEX: IfrNumericDisplay = IfrNumericDisplay { raw: 32 };
pub const IFR_OPTION_FLAGS_DEFAULT: IfrOptionFlags = IfrOptionFlags { raw: 16 };
pub const IFR_OPTION_FLAGS_DEFAULT_MFG: IfrOptionFlags = IfrOptionFlags { raw: 32 };
pub const IFR_ORDERED_LIST_FLAGS_UNIQUE_SET: IfrOrderedListFlags = IfrOrderedListFlags { raw: 1 };
pub const IFR_ORDERED_LIST_FLAGS_NO_EMPTY_SET: IfrOrderedListFlags = IfrOrderedListFlags { raw: 2 };
pub const IFR_STRING_FLAGS_MULTI_LINE: IfrStringFlags = IfrStringFlags { raw: 1 };
pub const IFR_SUBTITLE_FLAGS_HORIZONTAL: IfrSubtitleFlags = IfrSubtitleFlags { raw: 1 };
pub const IFR_TIME_FLAGS_HOUR_SUPPRESS: IfrTimeFlags = IfrTimeFlags { raw: 1 };
pub const IFR_TIME_FLAGS_MINUTE_SUPPRESS: IfrTimeFlags = IfrTimeFlags { raw: 2 };
pub const IFR_TIME_FLAGS_SECOND_SUPPRESS: IfrTimeFlags = IfrTimeFlags { raw: 4 };
pub const IFR_TIME_FLAGS_STORAGE: IfrTimeFlags = IfrTimeFlags { raw: 48 };
pub const IFR_TIME_STORAGE_NORMAL: IfrTimeStorage = IfrTimeStorage { raw: 0 };
pub const IFR_TIME_STORAGE_TIME: IfrTimeStorage = IfrTimeStorage { raw: 16 };
pub const IFR_TIME_STORAGE_WAKEUP: IfrTimeStorage = IfrTimeStorage { raw: 32 };
pub const IFR_NUMERIC_FLAGS_SIZE_MASK: u8 = 3;
pub const IFR_NUMERIC_FLAGS_DISPLAY_MASK: u8 = 48;
