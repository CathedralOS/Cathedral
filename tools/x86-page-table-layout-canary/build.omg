// Isolated pure-fact frontier for Cathedral's first x86 paging-word layout.

machine build(builder: &mut Build) {
    builder.application("cathedral-x86-page-table-layout-canary");
    builder.freestanding = true;
    builder.depend_as("facts", Source::Path {
        location: "facts"
    });
}
